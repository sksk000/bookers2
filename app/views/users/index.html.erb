<div class="container">
    <div class="row">
        <div class="col-md-3">
          <%= render "info", user: current_user %>
          <%= render "books/index" %>
        </div>
        <div class="col-md-8">
          <h2>Users</h2>
          <table class="table">
            <thead>
              <tr>
                <th scope="col">image</th>
                <th scope="col">name</th>
              </tr>
            </thead>
            <tbody>
              <% @user.each do |data| %>
                <tr>
                  <% if data.image.attached? %>
                    <td><%= image_tag data.image, size: "150x150" %></td>
                  <% else %>
                    <td><%= image_tag "no_image", size: "150x150" %></td>
                  <% end %>
                  <td><%= data.name %></td>
                  <td><%= link_to "Show", user_path(data.id) %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
    </div>
</div>