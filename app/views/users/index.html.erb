<div class="container">
    <div class="row">
        <div class="col-md-3">
          <%= render "info" %>
          <%= render "books/index" %>
        </div>
        <div class="col-md-8">
          <h2>Users</h2>
          <table class="table">
            <thead>
              <tr>
                <th scope="col">image</th>
                <th scope="col">name</th>
              </tr>
            </thead>
            <tbody>
              <% @user.each do |data| %>
                <tr>
                  <td><%= image_tag data.image, size: "150x150" %></td>
                  <td><%= data.name %></td>
                  <td><%= link_to "Show", user_path(data.id) %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
    </div>
</div>