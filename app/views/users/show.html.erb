<div class="container">
    <div class="row">
        <div class="col-md-3">
          <%= render "info" %>
          <%= render "books/index" %>
        </div>
        <div class="col-md-8">
          <h2>Books</h2>
          <table class="table">
            <thead>
              <tr>
                <th score="col"></th>
                <th scope="col">Title</th>
                <th scope="col">Opinion</th>
              </tr>
            </thead>
            <tbody>
              <% @user.books.each do |data| %>
                <tr>
                  <td><%= image_tag data.user.image, size: "100x100" %></td>
                  <td><%= link_to data.title, book_path(data.id)%></td>
                  <td><%= data.body %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
    </div>
</div>