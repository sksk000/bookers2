<div class="container">
    <div class="row">
        <div class="col-md-3">
          <%= render "users/info" %>
          <%= render "index" %>
        </div>
        <div class="col-md-8">
          <h2>Books</h2>
          <table class="table">
            <thead>
              <tr>
                <th scope="col">Title</th>
                <th scope="col">Opinion</th>
              </tr>
            </thead>
            <tbody>
              <% @book.each do |data| %>
                <tr>
                  <td><%= link_to data.title, book_path(data.id)%></td>
                  <td><%= data.body %></td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
    </div>
</div>